import React from 'react';
import { ListView, GridItem } from '../components/ListView';

export default () => {
    return (
        <ListView items={
            []
        } renderItem={
            (item) => (

            )
        }
        />
    );
};