import React from 'react';
/** @jsx jsx */
import { jsx } from '@emotion/core';

const recordPanelStyles = (theme: Theme) => {
    return {};
};

export default () => {
    return (
        <div css={recordPanelStyles}>

        </div>
    );
};