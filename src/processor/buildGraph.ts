export const buildAlien = (ctx: AudioContext, options: any) => {

};

export const buildAstronaut = (ctx: AudioContext, options: any) => {

};

export const buildBalrog = (ctx: AudioContext, options: any) => {};

export const buildChild = (ctx: AudioContext, options: any) => {};

export const buildFemale = (ctx: AudioContext, options: any) => {};

export const buildMale = (ctx: AudioContext, options: any) => {};

export const buildOldFemale = (ctx: AudioContext, options: any) => {};

export const buildOldMale = (ctx: AudioContext, options: any) => {};

export const buildRobot1 = (ctx: AudioContext, options: any) => {};

export const buildRobot2 = (ctx: AudioContext, options: any) => {};

export const buildTransformer = (ctx: AudioContext, options: any) => {};

export const buildUncle = (ctx: AudioContext, options: any) => {};

export const buildBroadRoom = (ctx: AudioContext, options: any) => {};

export const buildCave = (ctx: AudioContext, options: any) => {};

export const buildHall = (ctx: AudioContext, options: any) => {};

export const buildUnderWater = (ctx: AudioContext, options: any) => {};

export const buildMegaphone = (ctx: AudioContext, options: any) => {};

export const buildMuffler = (ctx: AudioContext, options: any) => {};

export const buildRadio = (ctx: AudioContext, options: any) => {};

export const buildTelephone = (ctx: AudioContext, options: any) => {};